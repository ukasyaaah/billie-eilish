export default defineAppConfig({
  ui: {
    colors: {
      primary: "lime",
    },
    pageFeature: {
      slots: {
        root: "font-sans",
        title: "text-primary",
        description: "text-white",
      },
    },
  },
});
